version: "3.9"

services:
  app:
    build: .
    restart: always
    ports:
      - 8000:8000
    volumes:
      - .:/code
      - node_modules:/code/node_modules
    command: >
      sh -c "cd /code &&
             npm run build &&
             conda run -n CARdeploy python manage.py migrate && 
             conda run -n CARdeploy python manage.py wait-for-db &&
             conda run -n CARdeploy python manage.py makemigrations &&
             conda run -n CARdeploy python manage.py runserver 0.0.0.0:8000"
    env_file:
      - ./.env
    depends_on:
      - db
      - redis
      - rabbit

  db:
    image: postgres:10-alpine
    restart: on-failure
    env_file:
      - ./.env
    volumes:
      - pgdata:/var/lib/postgresql/data/

  redis:
    image: redis:alpine

  celery:
    restart: always
    build: .
    command: >
      sh -c "cd /code && 
             conda run -n CARdeploy celery -A CAR worker -l info"
    volumes:
      - .:/code
    env_file:
      - ./.env
    depends_on:
      - redis
      - rabbit

  rabbit:
    image: rabbitmq:alpine
    restart: on-failure
    env_file:
      - ./.env
    ports:
      - 5672:5672
      - 15672:15672

volumes:
  pgdata:
  node_modules:
